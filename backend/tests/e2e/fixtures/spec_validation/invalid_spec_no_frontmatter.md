# SPEC-0003: No Frontmatter

**Sprint 126 E2E Test Fixture** - This spec has no YAML frontmatter block.

## Overview

This specification is completely missing the YAML frontmatter section.

## Expected Violations

All frontends should detect:
- SPC-004: Missing YAML frontmatter block
